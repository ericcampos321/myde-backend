/**
 * Schema Drizzle do domínio.
 *
 * A modelagem real (tenants, contacts, conversations, messages) entra em um
 * commit dedicado. Mantido vazio aqui para a fundação compilar sem inventar
 * modelagem antes da hora.
 */
export {};
